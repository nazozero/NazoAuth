# NazoAuth

Rust 身份认证与授权服务；服务端、主机控制器 NazoAuthCtl、界面 NazoAuthWeb 保持独立职责。

## 约束

- 从需求、不变量、状态归属和失败模式决定方案；跨边界修改先追踪受影响链路。按真实职责与生命周期组织代码，禁止重复实现、无消费者抽象和纯转发层，不按行数拆文件。
- 领域与应用不依赖宿主、HTTP 框架或存储驱动；适配器实现端口，宿主负责装配和资源生命周期。协议、权限和状态转换只有一个权威实现，缓存与投影不得取代事实源。
- 保持租户隔离、请求授权、原子消费与防重放；未知安全状态不得放行，秘密不得进入日志或公开产物。服务端不能接管控制器的独立恢复职责。
- 分析任务只调查；实现任务完成修改和必要验证，保护已有工作。提交、推送、合并、部署、发布及其他持久外部操作需用户授权，已有授权不重复询问；子代理仅按用户要求使用。
- **每次修改同步更新对应文档、示例、受影响译文及索引。** 文档与实现不符时查明并修正；历史记录保留原版本与证据边界。

## 按需读取

只读取本次任务对应的文档及其必要引用，不要求预读全库。

| 任务 | 文档 |
| --- | --- |
| crate、状态与依赖边界 | [架构](docs/project/architecture.md) |
| 协议、权限与能力 | [规范矩阵](docs/protocol/rfc-compliance-matrix.md)、[能力策略](docs/protocol/composable-capability-policy.md) |
| 配置、存储、部署与恢复 | [文档索引](docs/README.md)中的对应专题 |
| 控制器契约 | [仓库分工](docs/operations/controller-repository-split.md) |
| 测试、构建与验收 | [验证指南](docs/project/testing.md) |

验证与改动风险匹配；必要检查通过后不重复扩大验证。区分代码、测试、正式一致性、部署和性能证据，缺少必要证据不得报告完成。
